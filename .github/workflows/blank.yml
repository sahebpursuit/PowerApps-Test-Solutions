- name: Check PAC Auth Status
  shell: pwsh
  run: |
    # This shows who the CLI thinks it is
    pac auth list
    
    # This tests if the current identity can actually "see" the environment
    pac org who --url ${{ secrets.ENV_URL }}
